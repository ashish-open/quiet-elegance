const HeroSection = () => {
  return (
    <section 
      className="min-h-[90vh] flex flex-col items-center justify-center bg-charcoal relative bg-cover bg-center bg-no-repeat"
      style={{
        backgroundImage: 'url(/image.png)',
      }}
    />
  );
};

export default HeroSection;
